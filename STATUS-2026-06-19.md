# navi-connect — Project Status (auto-check 2026-06-19)

*Automated scheduled check. No clarifying questions possible — observations below are derived from the docs (`HANDOFF.md`, `ROADMAP-V2.md`, `DESIGN-adaptive-audiomuse.md`, `MEMORY.md`) and file modification times. Note: none of the repos are under git here (`feishin` has an empty `master`, others have no `.git`), so "recent changes" = filesystem mtimes, not commits.*

## Where the project is

The core remote-control platform is **done and user-confirmed**. Active work has fully moved on to the **AudioMuse-AI recommendation / adaptive-radio layer** described in `DESIGN-adaptive-audiomuse.md`.

**Stable / confirmed working (per HANDOFF):**
- Hub + transfer-with-resume; Feishin ⇄ Navic ⇄ Chromecast transfers.
- Feishin unified playerbar + side queue, startup-runaway fix, remote Auto DJ, cast bridge (incl. dead-socket recovery).
- Navic unified player (mini + now-playing + queue), Android media controls driving the remote session.
- Roadmap-V2 Phase 1/2 (ratings/favorites, similar-songs radio, smart-playlist editor, playlist downloads, metered transcoding) — mostly built.

## Recent changes (last ~4 days)

The adaptive AudioMuse build (DESIGN doc §8 build order) is well underway in **both** clients — this is clearly the current focus:

**Feishin (most recent edits Jun 18):**
- Tier-2 core API proxy in the main process — `main/features/core/audiomuse/index.ts` + `preload/audiomuse.ts` (alchemy / fingerprint handlers, fail-soft, returns only resolved song ids).
- Mood Flow feedback engine — `auto-dj/mood-flow-signals.ts` + `hooks/use-mood-flow-signals.ts` (play-fraction → ADD/SUBTRACT signals).
- `use-auto-dj.ts` / `audio-muse-source.ts` wired for the new top-up modes.
- "Make a journey" context action — `context-menu/actions/play-track-journey-action.tsx`.
- **Adaptive visualizer in progress** — `visualizer/components/blob/visualizer.tsx` (the metaball/mesh-gradient, newest file overall, Jun 18 21:48) + `use-track-mood.ts`. This is DESIGN §7, the last planned phase.

**Navic (most recent edits Jun 16):**
- `AudioMuseManager.kt`, `RadioManager.kt`, autoplay model (`AutoplayMode.kt`), character presets (`MoodCharacter.kt`).
- `AdaptiveMoodBackground.kt` + `NowPlayingAutoplaySelector.kt` (adaptive visualizer + the 4-mode autoplay control on the now-playing screen).
- Radio entry points added to `SongSheet.kt` / `SongRowDropdown.kt`.

So against DESIGN §8: steps 1–3 (SonicEngine + Tier-1 features + Tier-2 behind the probe) are largely landed; **step 4 (Adaptive "Mood Flow" mode) is mid-build**; **step 5 (adaptive visualizer) is actively being built right now** in both clients.

*(Side project `lb-bot/` — the ListenBrainz bot — was last touched Jun 12 and is not part of the navi-connect roadmap.)*

## Recommended next steps

1. **Verify the server-side Tier-2 prerequisite.** The whole adaptive/fingerprint/alchemy path needs the AudioMuse plugin installed + enabled and a warm library index on `music.example.com`. DESIGN §9 flags this as still-open. Probe via `getOpenSubsonicExtensions` (should advertise `sonicSimilarity`) before trusting any Tier-2 testing — otherwise the sonic endpoints 404 and you'll be debugging a server config, not client code.

2. **Finish the Mood Flow re-splice loop (DESIGN §5).** Feishin currently tops up at queue-refill time, not the "discard the un-played tail and splice fresh candidates ahead of the playhead" behavior. Land the proper re-splice + character-param wiring (`temperature` / `subtract_distance`) so Echo Match / Steady Vibes / Transition Maestro actually bias the engine.

3. **Close the Feishin↔Navic parity check on the adaptive layer.** Confirm both clients read the same shared character/preset catalog and route mixes through the existing hub plumbing (so a mood-flow session started on one device plays on the active device). Then have the user do a release build of Navic (can't compile in sandbox) and validate the 4-mode selector end-to-end.

4. **Complete the adaptive visualizer (DESIGN §7) — the last item.** Feishin: finish the blob/mesh-gradient swap-in (palette ← centroid, motion ← `other_features`/amplitude) and ensure it reverts to the standard visualizer when leaving Adaptive mode. Navic: same palette/motion spec driving `AdaptiveMoodBackground` / `BlendBackground`.

5. **Routine hygiene before each handoff:** run the two `tsc` typecheck commands after any Feishin change (not `pnpm run typecheck`), and update `MEMORY.md` — its index still says "building hub next," which is well out of date.

## Risks / watch-items
- No version control on these repos in this environment — consider committing, since the codebase is now large and multi-feature.
- Tier-2 features must stay fail-soft (grey-out + fall back to Tier-1) if the index is cold or the plugin is missing.
- Feishin installer remains AV-blocked → portable build only; cast URLs must use `https://music.example.com` (not Tailscale IPs).
